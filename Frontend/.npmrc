# Défense supply-chain : refuse tout paquet publié il y a moins de 24h.
# Si une version compromise est publiée sur npm, la communauté a typiquement
# le temps de la détecter et de la retirer avant qu'elle ne soit installée ici.
# Valeur en minutes (1440 = 24h). Requiert pnpm >= 10.
minimum-release-age=1440

# Pas d'exception pour les paquets propres (aucun ici), à étendre si besoin.
# minimum-release-age-exclude=@zenix/*

# Force l'engine déclarée (node 20+) au lieu de juste warner.
engine-strict=true

# Évite les approvisionnements interactifs en CI.
fund=false
audit=false
update-notifier=false

# Empêche pnpm d'exécuter les scripts post-install des dépendances sans approbation
# explicite (postinstall = vecteur classique de compromission supply-chain).
# Les paquets approuvés vont dans pnpm.onlyBuiltDependencies de package.json.
ignore-scripts=true
